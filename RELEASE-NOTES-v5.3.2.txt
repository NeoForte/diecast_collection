Pocket 64 v5.3.2
===================

PHOTO VIEWER
- Returned the viewer to a simpler centered-photo presentation instead of forcing an oversized default image.
- Removed the FIT/FILL control entirely.
- Removed visible previous/next arrow buttons from the viewer.
- Keeps pinch-to-zoom and pan so the image can be enlarged to full-screen inspection when wanted.
- Keeps swipe navigation between photos at normal zoom using the existing viewer gesture logic.
- Uses a clean black backdrop and compact close button.
- Hides the photo counter when there is only one image.

STABILITY
- Replaced the behavior inside the legacy p64-v525-patch.js file itself so the old script URL cannot resurrect the FIT/FILL viewer.
- Preserves the Add Car Cancel/reset fix.
- No database, Supabase, auth, Sets, or saved-car schema changes.

VERSION
- Version bumped to 5.3.2.

BACKUP
- Full-project ZIP supplied for rollback/recovery.
