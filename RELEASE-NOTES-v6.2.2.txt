Pocket 64 v6.2.2 — Viewer Ownership Cleanup

- Removed the obsolete `editor-viewer-core.js` compatibility file after v6.2.1 device verification.
- Created permanent `viewer-core.js` ownership for the proven Photos-like viewer sizing and gesture behavior.
- Physically removed the duplicate Add Car cancel/reset fallback from the viewer layer; `editor-core.js` is now the single owner of that behavior.
- Removed the obsolete viewer-layer version-display override that still carried the historical 5.3.3 patch version.
- Updated the transitional bootstrap to load `viewer-core.js` directly.
- Updated architecture documentation to reflect current source ownership.
- No database, Supabase schema, collection-data, photo-data, Set-data, or backup-format migrations.

Test focus: photo viewer open/close, pinch/pan/swipe, Add Car cancel reset, Edit Car cancel safety, camera privacy indicator, Sets, Backup/Restore, and Settings version display.
