Pocket 64 — Emergency Login Recovery

This restores the last known-good 4.2.3 behavior from before the v5.0.0 experiment.

Keeps:
- Working login/auth flow
- Working Set ••• menu
- Working EDIT SET
- Main photo delete fix
- Photo viewer fixes

INSTALL
1. Replace the current sw.js with this sw.js.
2. Fully close Safari/Chrome/Pocket 64.
3. Reopen Pocket 64.
4. If the old frozen page is still showing, refresh once.

This recovery build intentionally does NOT include the v5 MutationObserver/version guard.
